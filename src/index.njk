---
title: Home
layout: layout.njk
---

<style>
  /* reduce top whitespace above hero ~75% */
  main { padding-top: 13px !important; }

  /* hero spacing below image ~60% reduction */
  .hero { margin-bottom: 16px; }

  /* two-column list on desktop, single column on mobile */
  .pa-list { 
    font-size: 1.1em; 
    line-height: 1.8; 
    margin: 0; 
    padding-left: 1.25rem; /* keep bullets aligned */
  }
  @media (min-width: 900px) {
    .pa-list {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      column-gap: 24px;
    }
    .pa-list li { margin-bottom: 0.2em; }
  }

  /* Latest from the Blog cards */
  .home-blog-list {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
  }
  .home-blog-card {
    border: 1px solid #e6e6e6;
    border-radius: 10px;
    padding: 12px 14px;
  }
  .home-blog-card h3 {
    margin: 0 0 6px 0;
    font-size: 1.05rem;
  }
  .home-blog-card p {
    margin: 0 0 8px 0;
    line-height: 1.5;
  }
  .home-blog-meta {
    font-size: 0.9rem;
    color: #666;
    margin-bottom: 6px;
  }
</style>

<div class="hero">
  <img src="/assets/lighthouse.jpg" alt="Lighthouse" style="width: 100%; border-radius: 8px;">
</div>

<h1 style="text-align: center; font-size: 2em; margin-bottom: 6px;">VMG Business Advisory, PLLC</h1>

<p style="text-align: center; font-size: 1.2em; margin-top: 4px;">
  Corporate & Transactional Counsel.<br>
  Practical legal and strategic guidance for businesses, founders, and investors.
</p>

<h2><a href="/practice-areas/" style="text-decoration: none; color: inherit;">Practice Areas</a></h2>

<ul class="pa-list">
  <li>Corporate Governance</li>
  <li>Private Placements &amp; Securities</li>
  <li>Mergers &amp; Acquisitions</li>
  <li>Venture Capital &amp; SAFE Negotiation</li>
  <li>Start-Up Counseling</li>
  <li>Professional Service Entities</li>
  <li>Not for Profit Entities</li>
  <li>Nuclear/SMR Energy Development</li>
  <li>Outside General Counsel Services</li>
</ul>

<hr style="margin: 36px 0;">

{% set posts = collections.blog | reverse %}

{% if posts | length > 0 %}
<section>
  <h2 style="margin-bottom: 12px;">Latest from the Blog</h2>

  <div class="home-blog-list">

    {# Post 1 #}
    {% if posts[0] %}
      {% set p = posts[0] %}
      <article class="home-blog-card">
        <h3>
          <a href="{{ p.url }}" style="text-decoration: none; color: inherit;">
            {{ p.data.title }}
          </a>
        </h3>
        <p class="home-blog-meta">
          {{ p.date | date("MMM d, yyyy") }}
        </p>
        {% set img0 = p.templateContent | firstImageSrc %}
        {% if img0 %}
          <a href="{{ p.url }}">
            <img src="{{ img0 }}" alt="{{ p.data.title }}" style="width: 50%; border-radius: 8px; margin-bottom: 6px;">
          </a>
        {% endif %}
        {% if p.data.description %}
          <p>{{ p.data.description }}</p>
        {% endif %}
      </article>
    {% endif %}

    {# Post 2 #}
    {% if posts[1] %}
      {% set p = posts[1] %}
      <article class="home-blog-card">
        <h3>
          <a href="{{ p.url }}" style="text-decoration: none; color: inherit;">
            {{ p.data.title }}
          </a>
        </h3>
        <p class="home-blog-meta">
          {{ p.date | date("MMM d, yyyy") }}
        </p>
        {% set img1 = p.templateContent | firstImageSrc %}
        {% if img1 %}
          <a href="{{ p.url }}">
            <img src="{{ img1 }}" alt="{{ p.data.title }}" style="width: 50%; border-radius: 8px; margin-bottom: 6px;">
          </a>
        {% endif %}
        {% if p.data.description %}
          <p>{{ p.data.description }}</p>
        {% endif %}
      </article>
    {% endif %}

    {# Post 3 #}
    {% if posts[2] %}
      {% set p = posts[2] %}
      <article class="home-blog-card">
        <h3>
          <a href="{{ p.url }}" style="text-decoration: none; color: inherit;">
            {{ p.data.title }}
          </a>
        </h3>
        <p class="home-blog-meta">
          {{ p.date | date("MMM d, yyyy") }}
        </p>
        {% set img2 = p.templateContent | firstImageSrc %}
        {% if img2 %}
          <a href="{{ p.url }}">
            <img src="{{ img2 }}" alt="{{ p.data.title }}" style="width: 50%; border-radius: 8px; margin-bottom: 6px;">
          </a>
        {% endif %}
        {% if p.data.description %}
          <p>{{ p.data.description }}</p>
        {% endif %}
      </article>
    {% endif %}

    {# Post 4 #}
    {% if posts[3] %}
      {% set p = posts[3] %}
      <article class="home-blog-card">
        <h3>
          <a href="{{ p.url }}" style="text-decoration: none; color: inherit;">
            {{ p.data.title }}
          </a>
        </h3>
        <p class="home-blog-meta">
          {{ p.date | date("MMM d, yyyy") }}
        </p>
        {% set img3 = p.templateContent | firstImageSrc %}
        {% if img3 %}
          <a href="{{ p.url }}">
            <img src="{{ img3 }}" alt="{{ p.data.title }}" style="width: 50%; border-radius: 8px; margin-bottom: 6px;">
          </a>
        {% endif %}
        {% if p.data.description %}
          <p>{{ p.data.description }}</p>
        {% endif %}
      </article>
    {% endif %}

    {# Post 5 #}
    {% if posts[4] %}
      {% set p = posts[4] %}
      <article class="home-blog-card">
        <h3>
          <a href="{{ p.url }}" style="text-decoration: none; color: inherit;">
            {{ p.data.title }}
          </a>
        </h3>
        <p class="home-blog-meta">
          {{ p.date | date("MMM d, yyyy") }}
        </p>
        {% set img4 = p.templateContent | firstImageSrc %}
        {% if img4 %}
          <a href="{{ p.url }}">
            <img src="{{ img4 }}" alt="{{ p.data.title }}" style="width: 50%; border-radius: 8px; margin-bottom: 6px;">
          </a>
        {% endif %}
        {% if p.data.description %}
          <p>{{ p.data.description }}</p>
        {% endif %}
      </article>
    {% endif %}

  </div>
</section>
{% endif %}
